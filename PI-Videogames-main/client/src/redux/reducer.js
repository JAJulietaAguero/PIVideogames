const initialState = {
    videogameDetail: [],
    allVideogames: [],
    videogameId: [],
    genres: []
}

const reducer = (state = initialState, action) => {
    switch(action.type) {
        default: 
        return {...state}
    }
}

export default reducer;